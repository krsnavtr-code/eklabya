// Standalone landing pages: these routes render WITHOUT the main site's
// Navbar and Footer. They are meant to be fully self-contained (e.g. ad
// campaign landing pages) and must not contain links back to the main site.
// To make a new page standalone, add its pathname to this list.
export const STANDALONE_PATHS: string[] = ["/sap-training-certification"];
